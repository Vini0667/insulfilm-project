{{#each errors}}
    <div class="alert alert-danger"> {{ text }} </div>
    {{else}}
{{/each}}

<div class="card">
    <div class="card-body">
        <form action="/client/register" method="post">
            <label for="name"> Informe o nome do cliente: </label>
            <input class="form-control" type="text" name="name" id="name" placeholder="Digite aqui o nome do cliente:">

            <label for="contact"> Informe o contato do cliente: </label>
            <input class="form-control" type="text" name="contact" id="contact" placeholder="Digite aqui o contato do cliente:">

            <button type="submit" class="btn btn-primary mt-4 position-absolute end-50"> Enviar </button>
        </form>
    </div>
</div>