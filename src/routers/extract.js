// Requiring packages
    const express = require(`express`);

// Requiring my files

// Defining variables
    const router = express.Router();

// Routes

// Get routes

// Post routes

module.exports = router;